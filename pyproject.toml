[project]
name = "opal-active-labeling-inference"
version = "0.1.0"
description = "Reproducible examples for the OPAL active-labeling inference paper"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "numpy",
    "scipy",
    "tqdm",
    "scikit-learn",
    "matplotlib",
    "cvxpy",
    "patsy",
    "ipykernel",
]

[tool.uv]
dev-dependencies = []
